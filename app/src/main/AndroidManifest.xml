<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.rahbod.habibi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pages.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pages.RegisterStepOneActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".pages.CheckCodeActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".pages.UserNameActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".pages.BillActivity" />
        <activity android:name=".pages.RequestStepOneActivity" />
        <activity android:name=".pages.RequestStepTowActivity" />
        <activity android:name=".pages.RequestStepThereActivity" />
        <activity android:name=".pages.RequestStepFourActivity" />
        <activity android:name=".pages.TransactionActivity" />
        <activity android:name=".pages.RequestListActivity" />
    </application>

</manifest>